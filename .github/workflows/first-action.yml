name: My First Action

on:
  push:
    branches:
      - main

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Message
        run: echo "Hello , GitHub Action chal gaya!"

      - name : Print Data
        run : date
      - name : Show directory
        run : pwd
      - name : List
        run : ls -la